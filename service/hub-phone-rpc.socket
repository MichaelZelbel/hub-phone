[Unit]
Description=hub-phone RPC socket (group-owned, so the assistant's sandboxed account can reach it without sudo)

[Socket]
ListenStream=/run/hub-phone-rpc.sock
SocketUser=root
SocketGroup=hub-phone
SocketMode=0660
Accept=yes

[Install]
WantedBy=sockets.target
